#include <QtWidgets>
#include "mainwindow.h"

MainWindow::MainWindow(QWidget *parent) : QWidget(parent) {
    // Create objects

    //QGridLayout* layout = new QGridLayout();
    // add objects
    // layout->addWidget(...)

    //setLayout(layout);
    //setWindowTitle("Bernina Embroidery Files Tool");

}

MainWindow::~MainWindow() {
    // delete objects
}
